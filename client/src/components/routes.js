// You can change the root url with no repeating to change root url
// For example, if you have to change from "/root/about" to "/about"
// you just need to declare const ROOT = "" .
const OZEN_SUSHI_ROOT = "";
const OZEN_KOREA_ROOT = "/ozen_korea";

const HOME = "/";
const ABOUT = "/about";
const MENU = "/menu";
const CONTACT = "/contact";

const OZEN_KOREA = "/ozen_korea";

export const routes = {
  ozen_sushi_root: OZEN_SUSHI_ROOT,
  ozen_korea_root: OZEN_KOREA_ROOT,
  home: HOME,
  about: ABOUT,
  menu: MENU,
  contact: CONTACT,
  ozen_korea: OZEN_KOREA
};
